Лабораторная работа №4
Задание 1
Напишите функцию, которая выполняет любые арифметические действия и выводит результат в консоль. Вызовите функцию с помощью «точки входа».

def main():
    print(2+2)

if __name__=='__main__':
    main()
Результат.
![](https://github.com/Sheanra/-/assets/140616211/1ede6672-e9ad-4630-b916-6ed53ca48691)

Задание 2
Функция «Напишите», которая выполняет любые арифметические действия, при помощи возвращает значение в то место, откуда вызывали функцию. Выведите результат в консоль. Вызовите функцию с помощью «точки входа».

def main():
    return 2+2
    

if __name__=='__main__':
    print(main())
def main():
    result= 2+2
    return result


if __name__=='__main__':
    answer=main()
    print(answer)
Результат.
![](https://github.com/Sheanra/-/assets/140616211/6c527c3f-7c9f-4ea3-bac0-990946cdc66e)

![](https://github.com/Sheanra/-/assets/140616211/9b52c3c5-2fd7-4cf3-8e61-6f4b97179fb8)

Задание 3
Напишите функцию, в которой передаются два аргумента, над которыми происходит арифметическое действие, результат возвращается туда, откуда эту функцию вызывали. Выведите результат в консоль. Вызовите функцию в любом небольшом цикле.

def main(one, two):
    result = one + two
    return result

for i in range (5):
    x=1
    y=10
    answer = main (x,y )
    print(answer)
def main(one, two):
    return one + two


for i in range (5):

    answer = main (one=1, two=10 )
    print(answer)
Результат.
![](https://github.com/Sheanra/-/assets/140616211/447adf42-f4e6-4373-b0cd-78eeb21bb52e)

![](https://github.com/Sheanra/-/assets/140616211/fa0e7e07-3fb3-4f3f-882a-511e46bb342b)

Задание 4
Запишите функцию, на вход которой подается какое-то изначально неизвестное количество аргументов, над которым будут проводиться арифметические действия. Для выполнения задания необходимо использовать кортеж «*args». На скриншоте ниже приведен пример такой программы с комментариями. Для определения понимания работы с кортежами рекомендуется поменять аргументы функции вызова, вручную подсчитать результат, только затем запустить программу с новыми значениями и проверить себя, наконец, вы поняли этот аспект программирования.

def main (x, *args):
    one = x
    two = sum(args)
    three = float(len(args))
    print(f"one{one}\ntwo={two}\nthree={three}")

    return x + sum (args)/ float(len(args))

if __name__ == '__main__':
    result = main (10, 0 ,1 ,2,-1,-2,-5,)
    print(f"\nresult={result}")
Результат.
![](https://github.com/Sheanra/-/assets/140616211/511115e6-a881-4434-8ddc-2adcda4effbf)

Задание 5
Напишите функцию, которая на вход получает кортеж «**kwargs» и с помощью цикла выводит значения, поступившие в функцию. На скриншоте ниже показаны два вызова функции вызова с «**kwargs» и два вызова работы с данными, поступившими в эту функцию. Комментарии в коде и теоретическая часть помогут вам разобраться в этом аспекте. Вызовите функцию с помощью «точки входа».

def main (**kwargs):
    for i in kwargs.items():
        print(i[0], i[1])

    print()

    for key in kwargs:
        print(f"{key}={kwargs[key]}")

if __name__ == '__main__':
    main(x=[1,2,3],y=[3,3,0],z=[2,3,0], q=[3,3,0],w=[3,3,0])
    print()
    main(**{'x': [1,2,3], 'y':[3,3,0]})
Результат.
![](https://github.com/Sheanra/-/assets/140616211/53d1b46c-682e-4500-a6d8-0ef2823bc133)

Задание 6
Напишите две функции. Первый – получает в виде параметра «**kwargs». Вторая считает среднее арифметическое из результатов первой функции. Вызовите первую функцию с помощью «точки входа» и минимум 4 аргументов.

def main (**kwargs):
    for i,j in kwargs.items():
        print(f"{i}. Mean = {mean(j)}")

def mean(data):
    return  sum(data)/ float(len(data))
    
if __name__ == '__main__':
    main(x=[1,2,3],y=[3,3,0])
Результат.
![](https://github.com/Sheanra/-/assets/140616211/b9cac04d-345d-40f9-bc82-b9d273b9c7f3)

Задание 7
создать дополнительный файл .py. Напишите в нем любую функцию, которая будет что угодно выводить в консоль, но не вызывайте ее в нем. Откройте файл main.py, импортируйте в него функцию из нового файла и с помощью «точки входа» вызовите эту функцию.

import say_hello
if __name__=='__main__':
    say_hello.say_hello()
def say_hello():
    print('Hello Dmitrii!')
Результат.
![](https://github.com/Sheanra/-/assets/140616211/a4d18db1-ef66-4778-8a95-0cb7c379a9a2)

Задание 8
Напишите программу, которая будет выводить корень, синус, косинус, полученного от числа пользователя.

import math

def main():
    value =int (input('Введите значение:'))
    print(math.sqrt(value))
    print(math.sin(value))
    print(math.cos(value))

if __name__== '__main__' :
    main()
from math import sqrt, sin , cos

def main():
    value =int (input('Введите значение:'))
    print(sqrt(value))
    print(sin(value))
    print(cos(value))

if __name__== '__main__' :
    main()
from math import *
def main():
    value =int (input('Введите значение:'))
    print(sqrt(value))
    print(sin(value))
    print(cos(value))

if __name__== '__main__' :
    main()
Результат.
![](https://github.com/Sheanra/-/assets/140616211/2c6a9fc0-db35-4a05-a358-dce3565a8aa0)
![](https://github.com/Sheanra/-/assets/140616211/760d3327-d591-4c3a-b9af-05a4a3e3bbab)
![](https://github.com/Sheanra/-/assets/140616211/c9d8b06b-d7cb-4e6a-9213-5f02e1b90056)

Задание 9
Напишите программу, которая будет рассчитывать, какой день недели будет через n-ное количество дней, которое укажет пользователь. В результате день недели указан в виде цифр, где 1 = понедельник, 2 = вторник, 3 = среда и так далее.

from datetime import datetime as dt
from datetime import timedelta as td

def main():
    print(
        f"Сегодня {dt.today().date()}."
        f"День недели - {dt.today().isoweekday()}"
    )
    n = int(input('Введите количество дней:'))
    today = dt.today()
    result = today + td(days=n)
    print(
        f"Через {n} дней будет {result.date()}."
        f"День недели - {result.isoweekday()}"
    )
if __name__ == '__main__':
    main()
Результат.
![](https://github.com/Sheanra/-/assets/140616211/49f67d6c-35b3-4cfe-a51f-89e5510a97ed)
![](https://github.com/Sheanra/-/assets/140616211/df89263f-4357-40d9-8487-f4074f199ce2)

Задание 10
global result

def rectangle():
    a=float(input("Ширина:"))
    b =float(input("Высота:"))
    global  result
    result= a*b

def triangle():
    a= float(input("Основание:"))
    h= float(input("Высота:"))
    global result
    result= 0.5 * a * h
figure = input("1-прямоуголник, 2- треугольник:")
if figure=='1':
    rectangle()
elif figure =='2':
    triangle()

print(f"Площадь: {result}")
Результат.
![](https://github.com/Sheanra/-/assets/140616211/cea94491-f3d1-476f-b05f-59549d81f003)




Самостоятельная работа №4
Задание 1
Дайте подробный комментарий к коду, написанному ниже. Комментарий нужен для каждой строчки кода, необходимо описать, что она делает. Не забывайте, что функции комментируются особенному

from datetime import datetime   # Импортирует из 'datetime' 'datetime'
from math import sqrt           # Импортирует из 'math' квадрат
def main(**kwargs):                                     # Обявление функции 'main' с аргументами '**kwargs' где **kwargs нужен для принятия словарей
    for key in kwargs.items():                          # Цикл для
        result = sqrt(key[1][0] ** 2 + key[1][1] ** 2)  # Форума пифагора
        print(result)                                   # Вывод в консоль результата
                                                        # Пустота
if __name__ == '__main__':                              # Точка входа
    start_time = datetime.now()                         # Начало таймера
    main(                                               # Обращение к фунцкции 'main'
        one=[10, 3],                                    # первый аргумент
        two=[5, 4],                                     # второй аргумент
        three=[15, 13],                                 # третий аргумент
        four=[93, 53],                                  # четвертый аргумент
        five=[133, 15]                                  # пятый аргумент
        )                                               # закрытая скобка
time_costs = datetime.now() - start_time                # конец таймера
print(f"Время выполнения программы {time_costs}")       # вывод в консоль время выполнения программы
                                                        # Конец кода
Результат.
![](https://github.com/Sheanra/-/assets/140616211/33262e4c-13f5-4b7a-b58e-16eee2c37661)

Вывод
Понял как и зачем используется if __name__ = "__main__" или же точка входа.

Задание 2
Напишите программу, которая будет заменять игровую кость с 6 гранями. Если значение равно 5 или 6, то в консоли выводится «Вы победили», если значения 3 или 4, то вы рекурсивно должны вызвать ту же функцию, если значение 1 или 2, то в консоли выводится «Вы победили». При этом каждый вызов функции необходимо выводить в консоль значение «кубика». Для выполнения задания необходимо использовать стандартную библиотеку Random. Программу нужно написать, используя одну функцию и «точку входа».

import random


def checkDice():
    dice = random.randint(1, 6)
    if dice == 3 or dice == 4:
        print("Переброс, вам выпало:", dice)
        random.randint(1, 6)
        checkDice()
    elif dice == 5 or dice == 6:
        print("Вы победили, вам выпало:", dice)

    elif dice == 1 or dice == 2:
        print("Вы проиграли, вам выпало", dice)


if __name__ == "__main__":
    checkDice()
Результат
![](https://github.com/Sheanra/-/assets/140616211/b12eba41-f987-45b3-ae9b-ff5eff1c4a20)

Вывод
Узнал о рекурсивных функциях.

Задание 3
Напишите программу, которая будет выводить текущее время с точностью до секунды в течение 5 секунд. Программу необходимо написать с использованием цикла. Подсказка: необходимо использовать модуль datetime и time, а также вам необходимо как-то «усыпать» программу на 1 секунду.

import time
from datetime import datetime


for i in range(5):
    now = datetime.now()
    current_time = now.strftime("%H:%M:%S")

    print(current_time)
    time.sleep(1)
Результат.
![](https://github.com/Sheanra/-/assets/140616211/71f28a45-38e9-4329-ab9e-ea6fa580124f)

Вывод
узнал про модуль datetimeи time.

Задание 4
Напишите программу, которая считает среднее арифметическое значение от аргументов вызываемой функции при условии, что изначальное количество этих аргументов неизвестно. Программе необходимо реализовать одну функцию и «точку входа».

test = [1, 2, 3]


def average(inpt):
    num_sum = 0
    count = 0
    for i in inpt:
        num_sum = num_sum + i
        count += 1

    return num_sum / count


if __name__ == "__main__":
    print(average(test))
Результат.
![](https://github.com/Sheanra/-/assets/140616211/76668c00-68e6-4166-af05-c250f3bb43a8)

Вывод
Узнал как делать функцию при неизвестном кол-во входных данных.

Задание 5
Создайте два файла Python, в одном будет выполняться вычисление квадрата треугольника с помощью формулы Герона (необходимо реализовать через функцию), а во вторую очередь будет переключаться взаимодействие с пользователем (получение всей имеющейся информации и вывод результатов). Напишите эту программу и переведите ее на консольную площадку.

main.py

import second

a = int(input("Введите 1-ю сторону: "))
b = int(input("Введите 2-ю сторону: "))
c = int(input("Введите 3-ю сторону: "))

print(second.Gerona(a, b, c))
второй.py

import math

def Gerona(a, b, c):
    p = (a+b+c)/2
    return math.sqrt(p*(p-a)*(p-b)*(p-c))
Результат.
![](https://github.com/Sheanra/-/assets/140616211/e9af500f-c09e-426e-bf10-f50ca4e5ae9b)

Вывод
Узнал о функциях обмена между 2-мя разными файлами кода.
